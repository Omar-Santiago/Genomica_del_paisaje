install.packages("rgee")
library(rgee)
ee_install()  # Instalar dependencias (necesario solo una vez)
# rgee nos permite conectarnos con google earth engine
# install.packages("rgee")
library(rgee)
ee_install()  # solo una vez
# rgee nos permite conectarnos con google earth engine
# install.packages("rgee")
library(rgee)
ee_check()    # Verificar instalación
ee_Initialize()  # Iniciar sesión en Earth Engine
ee_clean_pyenv()
ee_install()  # solo una vez
ee_clean_pyenv()
# rgee nos permite conectarnos con google earth engine
# install.packages("rgee")
library(rgee)
#ee_clean_pyenv()
ee_install()  # solo una vez
# rgee nos permite conectarnos con google earth engine
remotes::install_github("r-spatial/rgee")
library(rgee)
ee_install()  # solo una vez
ee_clean_pyenv()
# rgee nos permite conectarnos con google earth engine
remotes::install_github("r-spatial/rgee")
library(rgee)
ee_install()  # solo una vez
